# -*- coding: utf-8 -*-
"""mean_var_std.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1QE-vGiR5Al-ECRyO0NKf8MCAkJtj-9IL
"""

import numpy as np

def calculate(list):
    # Step 1: Validate input
    if len(list) != 9:
        raise ValueError("List must contain nine numbers.")

    # Step 2: Convert list to 3x3 NumPy array
    matrix = np.array(list).reshape(3, 3)

    # Step 3: Create dictionary with required calculations
    calculations = {
        'mean': [
            matrix.mean(axis=0).tolist(),
            matrix.mean(axis=1).tolist(),
            matrix.mean().tolist()
        ],
        'variance': [
            matrix.var(axis=0).tolist(),
            matrix.var(axis=1).tolist(),
            matrix.var().tolist()
        ],
        'standard deviation': [
            matrix.std(axis=0).tolist(),
            matrix.std(axis=1).tolist(),
            matrix.std().tolist()
        ],
        'max': [
            matrix.max(axis=0).tolist(),
            matrix.max(axis=1).tolist(),
            matrix.max().tolist()
        ],
        'min': [
            matrix.min(axis=0).tolist(),
            matrix.min(axis=1).tolist(),
            matrix.min().tolist()
        ],
        'sum': [
            matrix.sum(axis=0).tolist(),
            matrix.sum(axis=1).tolist(),
            matrix.sum().tolist()
        ]
    }

    return calculations

test_input = [0,1,2,3,4,5,6,7,8]
result = calculate(test_input)
print(result)